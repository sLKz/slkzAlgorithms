#include <iostream>

using namespace std;

int main()
{
    int matrice[30][30], n, m;
    int p, q;

    cout << "n = ";
    cin >> n;

    cout << "m = ";
    cin >> m;

    for (size_t i = 0; i < n; i++)
    {
        for (size_t j = 0; j < m; j++)
        {
            cout << "matrice[" << i << "][" << j << "] = ";
            cin >> matrice[i][j];
        }
    }

    cout << "\n";

    for (size_t i = 0; i < n; i++)
    {
        for (size_t j = 0; j < m; j++)
        {
            cout << matrice[i][j] << " ";
        }

        cout << "\n";
    }

    cout << "\n";

    do
    {
        cout << "p = ";
        cin >> p;

        cout << "q = ";
        cin >> q;
    } while (p < 1 or q < 1 or p > n or q > n);

    for (size_t j = 0; j < m; j++)
    {
        int aux = matrice[p - 1][j];
        matrice[p - 1][j] = matrice[q - 1][j];
        matrice[q - 1][j] = aux;
    }

    cout << "\n";

    for (size_t i = 0; i < n; i++)
    {
        for (size_t j = 0; j < m; j++)
        {
            cout << matrice[i][j] << " ";
        }

        cout << "\n";
    }

    return 0;
}
