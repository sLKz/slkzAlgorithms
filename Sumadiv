#include <iostream>
#include <fstream>

using namespace std;

int main()
{
  ofstream out("sumadiv.out");
  
  int n, s = 0;
  
  cout << "Nr. este: ";
  cin >> n;
  
  for (int i = 2; i <= n / 2; ++i)
  {
    if (n % i == 0)
    {
      s += i;
    }
  }
  
  out << s;
  
  return 0;
}
